import React, { memo } from 'react';
import { Text, View } from 'react-native';

const Settings = memo(() => {
  return (
    <View>
      <Text>Settings</Text>
    </View>
  );
});

export default Settings;
