import React, { memo } from 'react';
import { Text, View } from 'react-native';

const Export = memo(() => {
  return (
    <View>
      <Text>Export</Text>
    </View>
  );
});

export default Export;
