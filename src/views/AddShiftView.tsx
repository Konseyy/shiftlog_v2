import React, { memo } from 'react';
import { Text, View } from 'react-native';

const AddShift = memo(() => {
  return (
    <View>
      <Text>Add shift</Text>
    </View>
  );
});

export default AddShift;
